language: java
script:
  - ./gradlew check
  - ./gradlew selenium
jdk:
  - oraclejdk8
before_script:
  - ./gradlew run # start a Web server
  - sleep 3 # give Web server some time to bind to sockets, etc
